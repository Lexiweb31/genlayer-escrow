services:
  - type: web
    name: genlayer-escrow
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn dashboard.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DEPLOYER_PRIVATE_KEY
        sync: false
